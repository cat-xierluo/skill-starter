# Changelog

## 0.2.0 - 2026-07-30

### Changed

- 明确最小说明型与带脚本维护型两种 profile。
- 移除未被实现读取的 `API_KEY`、`MODEL_NAME`、`mode` 和 `max_items` 示例字段。
- 示例脚本开始读取 `assets/config.yaml.example`，并保留 CLI、环境变量覆盖顺序。
- 移除依赖 starter 父目录的相对链接，补独立 MIT 许可证。

## Unreleased

### Added

- Initial scaffold copied from `skill-starter`
